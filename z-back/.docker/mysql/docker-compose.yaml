version: '3'

services:
  db:
    image: mysql:8.0-debian
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: z
      MYSQL_USER: z
      MYSQL_PASSWORD: z
    ports:
      - 3306:3306